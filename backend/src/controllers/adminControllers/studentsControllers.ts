import { Request, Response } from "express";

import prisma from "../../config/db";

export const studentSchema = async (
  req: Request,
  res: Response
): Promise<void> => {
  try {
    cosnt;
  } catch (error) {}
};
